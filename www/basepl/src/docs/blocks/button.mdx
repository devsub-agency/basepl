---
title: Button
description: Basic and reusable Button block
---

## Installation

```bash
npx @basepl/cli add BaseplButton
```

Currently, basepl doesn't automatically update the payload types. So if you install the configuration and the component you have to update the types.
Simply run
```bash
pnpm generate:types
```

If not you will get an error in the BaseplButton/Component.tsx file. Because the BaseplButtonType is not present.

## Configuration

This is the config file for the button.

<ConfigPreview name='BaseplButton' description='A basic, reusable button for different blocks and components.' />

## Example

### Default large button

<ComponentPreview name='button-default' description="A large button with a default style." />

### Default large button

<ComponentPreview name='button-link' description="Button to be used as a link." />

### Default large button

<ComponentPreview name='button-external' description="Button with external reference." />
